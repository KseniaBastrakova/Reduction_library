#pragma once

namespace reduction_library{
namespace record{

    enum class Name{
        momentum,
        weighting,
        position,
        charge
    };

} // namespace record
} //namespace reduction_library
